import {Box, Grid} from '@mui/material'
import type {FC} from 'react'

const Processors: FC = () => {
  return (
    <Grid item xs={2}>
      <Box sx={{height: '100%'}} />
    </Grid>
  )
}

export default Processors
